## Summary

<!-- What does this change do and why? -->

## Kernel subsystem affected

<!-- e.g. net, block, fs, drivers/power, arm64 -->

## Source changes

<!-- Files changed and a short description of each -->

## Config changes

<!-- Kconfig / daisy_defconfig changes, if any (new CONFIG_* flags) -->

## Build result

- [ ] `./scripts/build_kernel.sh` completes
- [ ] `out/arch/arm64/boot/Image.gz-dtb` produced
- [ ] Banner shows the expected version (e.g. `4.9.337-DaisyForGaming`)

## Device tested

- Device / codename:
- Android version / ROM:
- Kernel version on device (`uname -r`):

## Logs

<!-- dmesg/logcat excerpts if relevant -->

## Performance / battery impact

<!-- Measured or expected impact -->

## Breaking changes

<!-- Anything that changes default behavior (default schedulers, defaults of
     toggles, config options)? -->

## Checklist

- [ ] I verified my change builds
- [ ] I tested on the device (or explicitly state why not)
- [ ] No secrets/credentials added
- [ ] Documentation updated if user-facing behavior changed
- [ ] Commit messages follow the repo conventions (`subsystem: summary`)
