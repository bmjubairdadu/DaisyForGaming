diff --git a/scripts/mkcompile_h b/scripts/mkcompile_h
index 0194060045e..eaa65040c34 100755
--- a/scripts/mkcompile_h
+++ b/scripts/mkcompile_h
@@ -67,15 +67,7 @@ UTS_TRUNCATE="cut -b -$UTS_LEN"
   echo \#define LINUX_COMPILE_BY \"`echo $LINUX_COMPILE_BY | $UTS_TRUNCATE`\"
   echo \#define LINUX_COMPILE_HOST \"`echo $LINUX_COMPILE_HOST | $UTS_TRUNCATE`\"
 
-# Also append owner repo branch last commit hash respectively after cc version
-  echo \#define LINUX_COMPILER \" \
-  `$CC -v 2>&1 | grep version | sed 's/(.*)//g'` :: \
-  `$LD -v 2>&1 | sed 's/(.*)//g;s/  / /g'` :: \
-  `git -C $SRC remote -v 2>&1 | grep push | head -n1 | cut -f2 | sed "s/(push)//" | cut -f4 -d "/"` // \
-  `git -C $SRC remote -v 2>&1 | grep push | head -n1 | cut -f2 | sed "s/(push)//" | cut -f5 -d "/"` // \
-  `git -C $SRC status 2>&1 | grep "On branch" | sed -e 's/On branch //g'` // \
-  `git -C $SRC rev-parse HEAD 2>&1` \
-  \"
+  echo '#define LINUX_COMPILER "DaisyForGaming by JUBAIR HOSEN"'
 ) > .tmpcompile
 
 # Only replace the real compile.h if the new one is different,
